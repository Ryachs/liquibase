databaseChangeLog:
  - include:
      file: db/changelog/tables/db.changelog-1.0.yaml
      # Modificamos este archivo para poder realizar mas de un cambio a la vez y hacemos la llamada a otro archivo que realiza la operacion.
      # de esta manera dejamos un registro de los cambios y operariones realizadas, (pudiendo reutilizarlas)